entryPoints:
  websecure:
    address: ":443"

tls:
  stores:
    default:
      defaultCertificate:
        certFile: "/certs/cert.crt"
        keyFile: "/certs/cert.key"

providers:
  docker:
    exposedByDefault: false

log:
  level: DEBUG
